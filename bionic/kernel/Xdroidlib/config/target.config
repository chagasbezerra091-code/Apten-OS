# Arquivo: bionic/kernel/Xdroidlib/config/target.config
# Configuracao do Kernel Apten OS para o Dispositivo Alvo Padrao (Target V1)

# --- Arquitetura e Plataforma ---
CONFIG_AP_ARCH_ARM64=y
CONFIG_AP_TARGET_DEVICE="AptenOS-Device-V1"
CONFIG_AP_CHIPSET_QCOM_AP700=y

# --- Xdroidlib e Core ---
CONFIG_AP_XDROIDLIB=y
CONFIG_AP_XDROIDLIB_VERSION_MAJOR=1
CONFIG_AP_XDROIDLIB_DEBUG=n             # Desabilita logs de DEBUG em builds de producao
CONFIG_AP_KERNEL_PANIC_TIMEOUT_MS=1000  # Tempo limite para dump de panico (1 segundo)

# --- Subsistemas de Memória ---
CONFIG_AP_VIRTUAL_MM=y
CONFIG_AP_PAGETABLE_4K=y
CONFIG_AP_SLAB_ALLOCATOR=y
CONFIG_AP_COMPACTION_ENABLED=y          # Habilita compactacao de memoria
CONFIG_AP_LOW_MEMORY_KILLER=y           # Habilita o LMK (Low Memory Killer)

# --- Drivers de Perifericos ---
# Drivers essenciais para o AptenOS-Device-V1
CONFIG_AP_DRIVER_GPIO=y
CONFIG_AP_DRIVER_USB_3_1=y
CONFIG_AP_DRIVER_DISPLAY_APTEN_OLED=y   # Driver da tela que a sua UI usara
CONFIG_AP_DRIVER_WIFI_INTEL_XX=m        # Driver como modulo (m) para carregamento dinamico
CONFIG_AP_DRIVER_GPU_APTEN_VULKAN=y

# --- Rede ---
CONFIG_AP_NET_IPV6=y
CONFIG_AP_NET_FIREWALL=y
CONFIG_AP_NET_TLS_HW_ACCEL=y            # Aceleracao de hardware para TLS (essencial para Google Play)

# --- Aplicacoes e Services ---
CONFIG_AP_SERVICE_SECURITY_MANAGER=y
CONFIG_AP_SERVICE_AUTOMATION_TEST=n     # Oculta o servico de automacao em builds de producao (somente em debug)
CONFIG_AP_APP_EMAIL=y
CONFIG_AP_APP_SETTINGS=y
